version: 2

sources:
  - name: source              # Este es el nombre que dbt usará internamente
    description: "Datos crudos en el esquema public"
    database: postgres     # <--- CAMBIA ESTO por el nombre de tu BD en Postgres
    schema: public
    tables:
      - name: datos_embalses_crudos
      - name: maestro_regiones

- name: gcp_source
    database: "{{ env_var('GCP_PROJECT_ID') }}" 
    schema: dbt_test 
    tables:
      - name: datos_embalses_crudos
      - name: maestro_regiones